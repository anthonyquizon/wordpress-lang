#lang racket

;; variable 

;; create [path] [db]
  ;; rm
  ;; mkdir
  ;; config
  ;; install
  ;; db drop
  ;; db create

;; uploads
  

;; plugin
  ;; activate

;; theme 
  ;; run generator to tmp output
  ;; cp tmp into webroot/wp-content/theme
  ;; activate

#|

|#

